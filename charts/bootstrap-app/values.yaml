bootstrap:
  stacks:
    apps:
      enabled: true

  applications:
    timeoff:
      enabled: true
      stacks: ["apps"]
      namespace: app
      orgname: luisito666
      repo: timeoff-management-application
      branch: master
